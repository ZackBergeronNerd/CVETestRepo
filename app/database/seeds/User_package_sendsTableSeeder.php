<?php

class User_package_sendsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('user_package_sends')->truncate();

		$user_package_sends = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('user_package_sends')->insert($user_package_sends);
	}

}
