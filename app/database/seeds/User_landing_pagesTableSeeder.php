<?php

class User_landing_pagesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('user_landing_pages')->truncate();

		$user_landing_pages = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('user_landing_pages')->insert($user_landing_pages);
	}

}
