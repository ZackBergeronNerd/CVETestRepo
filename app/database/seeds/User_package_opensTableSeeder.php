<?php

class User_package_opensTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('user_package_opens')->truncate();

		$user_package_opens = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('user_package_opens')->insert($user_package_opens);
	}

}
