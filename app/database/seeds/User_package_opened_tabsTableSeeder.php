<?php

class User_package_opened_tabsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('user_package_opened_tabs')->truncate();

		$user_package_opened_tabs = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('user_package_opened_tabs')->insert($user_package_opened_tabs);
	}

}
